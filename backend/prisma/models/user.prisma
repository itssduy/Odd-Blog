model User {
    id String @id @default(uuid())
    email String?
    username String @unique
    salt String
    hash String
    posts Post[]
    comments Comment[]
    created_at DateTime @default(now())
}